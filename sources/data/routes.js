/**
 * Recorded roads between the landing and each area.
 *
 * The world has no road network to query, so these are captured by driving:
 * press R to start recording, drive the route you want visitors taken along,
 * press R again and the waypoints are printed to the console and copied to the
 * clipboard, ready to paste in here.
 *
 * Keys match the destination names in Autopilot.getDestinations(). A missing
 * or empty route falls back to heading straight at the target.
 */
export default {
    visionLab: [],
    aiLab: [],
    projects: [],
    career: [],
    lab: [],
    social: [],
}
